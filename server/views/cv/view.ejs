<% include ../partials/header %>
<% include ../partials/navbar %>
<% include ../loader/transLoad %>
<!-- Page Header -->
<div class="fluid-container project-top-header">
    <div class="row">
        <div class="col-12">
            <div class="project-banner text-center" style="background-image: url('images/profileCarousel/p1.jpg')"></div>
        </div>
    </div>
    <div class="row  controls-row text-light no-gutters bg-light text-dark">
        <div class="col-2">
            <a class="btn btn-lg btn-link bg-light text-dark btn-back" href="#" onclick="history.go(-1)"><i class="fas fa-arrow-left"></i></a>
        </div>
        <div class="col-8 text-center">
            <% if (message.success || message.failure) { %>
                <% if (message.success) { %>
                        <h5 class="text-center card-header typer">
                                <div class="typewriter">
                                    <%= message.success %>
                                </div>
                            </h5>
                    
                    <% } %>
                    <% if (message.failure) { %>
                        <h5 class="card-header typer">
                                <div class="text-center typewriter bg-danger">
                                    <%= message.failure %>
                                </div>
                            </h5>
                        <% } %>
            <% } else { %>
                <h5 class="card-header typer">
                        <div class="text-center typewriter">
                            <%= title.toUpperCase() %>
                        </div>
                    </h5>
                
            <% } %>
        </div>
            <div class="col-2"></div>
        </div>
    </div>
    <!-- Content & Title -->
    <div class="container bio-content-container">
        <h5>HI!</h5>
        <div class="row cv-bio-row">
            <div class="col-12 text-dark">
                <p><%- user.bio %></p>
                <p>Best Regards,</p>
                <p class="autograph"><%= user.firstName%> <%= user.lastName%></p>
                <hr />
                <div class="row cv-controls">
                    <div class="col">
                        <a href="/projects" class="cv-controls-btn btn btn-success">Project Gallery</a>
                    </div>
                    <div class="col">
                        <% if (jobs.length > 0) { %>
                            <button class="cv-controls-btn btn btn-info" onclick="document.getElementById('workxp').scrollIntoView({behavior:'smooth', block: 'start', inline: 'nearest'})">Work Experience</button>
                            <% } %>
                        </div>
                        <div class="col">
                            <button class="cv-controls-btn btn btn-dark text-light" onclick="document.getElementById('education').scrollIntoView({behavior:'smooth', block: 'start', inline: 'nearest'})">Education</button>
                        </div>
                        <div class="col">
                            <!-- CONTACT COLLAPSE -->
                            <button class="cv-controls-btn btn btn-danger" type="button" data-toggle="collapse" data-target="#contactCollapse" aria-expanded="false" aria-controls="contactCollapse">
                                Contact
                            </button>
                        </div>
                    </div>
                    <div class="collapse" id="contactCollapse">
                        <div class="card card-body">
                            <form action="/" method="POST">
                                <%  include ../forms/message %>
                                <button class="btn btn-danger" type="submit">Send</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- WORK -->
        <div class="container cv-jobs bg-dark">
            <% if (jobs.length > 0) { %>
                <h3 id="workxp" class="text-light">Work Experience</h3>
                <% jobs.forEach((job)=>{ %>
                    <div class="card job-card bg-dark">
                        <div class="card-header bg-info text-light">
                            <%- job.title.toUpperCase() %>
                        </div>
                        <div class="card-body bg-light text-dark job-body">
                            <h4 class="card-title"><strong>@ </strong><%- job.company %></h4>
                            <h5 class="card-text"><strong>TL;DR</strong></h5>
                            <p><%- job.description %></p>
                            <!-- COLLAPSE -->
                            <button class="btn btn-info" data-toggle="collapse" href="#collapse<%= job.id %>" role="button" aria-expanded="false" aria-controls="collapse<%= job.id %>" onclick="setTimeout(()=> {document.getElementById('collapse<%= job.id %>').scrollIntoView({behavior:'smooth', block: 'start', inline: 'nearest'}) }, 300)" >More...</button>
                            <div class="row">
                                <div class="col">
                                    <div class="collapse multi-collapse collapse-wrapper" id="collapse<%= job.id %>">
                                        <div class="card card-body job-features-wrapper">
                                            <!-- Features -->
                                            <h6 class="collapse-header bg-dark text-light">Features</h6>
                                            <% if (job.features) { %>
                                                <ul class="list-group">
                                                    <% job.features.forEach((feature)=>{ %>
                                                        <% if (feature.length > 0) { %>
                                                            <li class="list-group-item"><%- feature %></li>
                                                            <% } %>
                                                            <% }) %>
                                                        </ul>
                                                        <% } %>
                                                        <% if (job.content){ %>
                                                            <h6 class=" collapse-header bg-dark text-light">About</h6>
                                                            <p class="card-text job-content bg-light text-dark"><%- job.content %></p>
                                                            <% } %>
                                                        </div>
                                                        <button class="btn btn-danger btn-block" data-toggle="collapse" href="#collapse<%= job.id %>" role="button" aria-expanded="false" aria-controls="collapse<%= job.id %>" onclick="setTimeout(()=> {document.getElementById('collapse<%= job.id %>').scrollIntoView({behavior:'smooth', block: 'start', inline: 'nearest'}) }, 300)" >Close</button>
                                                    </div>
                                                </div>
                                            </div>
                                            <!-- DATES -->
                                            <% if (job.start_date || job.end_date) {%>
                                                <div class="card-footer bg-secondary text-light job-footer">
                                                    <% if (job.start_date) { %>
                                                        <span class="job-date-text">FROM</span><%= job.start_date %>  
                                                        <% } %>
                                                        <% if (job.end_date) { %>
                                                            <span class="job-date-text">TO</span><%= job.end_date %>
                                                            <% } %>
                                                        </div>
                                                        <% } %>
                                                    </div>
                                                    <% }) %>
                                                </div>
                                                <% } %>
                                            </div>
                                            <!-- Education & Skills-->
                                            <div class="container edu-skills-container">
                                                
                                                <div class="row">
                                                    <div class="col-lg-6 col-md-6 col-sm-12 edu-skills-col">
                                                        <h3 id="eduction" class="text-light">Education</h3>
                                                        <ul class="list-group features-list">
                                                            <li id="education" class="list-group-item feature-item bg-warning text-light"></li>
                                                            <li class="list-group-item feature-item">Sound School- 3 Years Sound Engineering</li>
                                                            <li class="list-group-item feature-item">Udemy -Web Developer Bootcamp</li>
                                                            <li class="list-group-item feature-item">Udemy - Python Bootcamp</li>
                                                        </ul>
                                                    </div>
                                                    <div class="col-lg-6 col-md-6 col-sm-12 edu-skills-col">
                                                        <h3 id="skills" class="text-light">Skills</h3> 
                                                        <ul class="list-group features-list">
                                                            <li class="list-group-item feature-item bg-danger text-light"></li>
                                                            <li class="list-group-item feature-item">ProTools, Cubase, Ableton</li>
                                                            <li class="list-group-item feature-item">Photoshop, Premire</li>
                                                            <li class="list-group-item feature-item">Full Stack NodeJS, Express, MongoDB, Postgres</li>
                                                        </ul>
                                                    </div>
                                                </div>
                                            </div>
                                            
                                            <script src="../../scripts/transloader.js"></script>
                                            <% include ../partials/footer %>